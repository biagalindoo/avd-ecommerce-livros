[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "avd-ecommerce-livros"
version = "0.1.0"
description = "Projeto final de Analise e Visualizacao de Dados com Python."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
